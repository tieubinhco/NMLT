#include <stdio.h>
#include <stdlib.h>
#include <windows.h>




void main()
{
    int so;
    printf("Hay nhap mot so: ");
    scanf("%d", &so);

    printf("\nHexadecimal number   %X\n", so);
    printf("Octal number         %o\n", so);
    printf("Decimal number       %d\n", so);

}

